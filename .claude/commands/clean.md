Clean up the repository by removing build artifacts:

1. Remove the build_lily58/ directory
2. Remove any .uf2, .hex, .bin files in the root
3. Remove any .o, .a, .so object files
4. Remove .build/ directory if present
5. Show what was removed

This helps start fresh for new builds and reduces repository clutter.
